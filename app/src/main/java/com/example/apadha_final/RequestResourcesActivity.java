/* RequestResourcesActivity.java - Allows users to request food, medicine, or other resources */
package com.example.apadha_final;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class RequestResourcesActivity extends AppCompatActivity {
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_request_resources);
    }
}